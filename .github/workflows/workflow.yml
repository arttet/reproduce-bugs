name: Tests

on: [push]

jobs:
  build:
    name: ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-10.15, macos-11.0, macos-latest]
    steps:
      - name: Upgrade Homebrew
        run: brew update && brew upgrade
